name: scrutora

on:
  push:
    branches:
      - main
      - compliant

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4

      # TODO: confirm the real secret name and scan invocation against
      # https://www.scrutora.com/docs/ci-cd with real product access before
      # relying on this job. The docs fetch during this build returned a
      # secret name and API host that didn't match the Scrutora brand
      # (SECUREHEALTH_API_KEY / api.scrutora.com), which does not look
      # trustworthy, so a placeholder is used here instead.
      - name: Run Scrutora scan
        env:
          SCRUTORA_API_KEY: ${{ secrets.SCRUTORA_API_KEY }}
        run: |
          echo "TODO: replace with the real Scrutora CLI/action invocation"
          echo "placeholder-sarif" > results.sarif

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
